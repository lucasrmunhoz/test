#include <stdio.h>

int main() {
    float x, y;

    printf("Digite a coordenada x: ");
    scanf("%f", &x);
    printf("Digite a coordenada y: ");
    scanf("%f", &y);
    printf("As coordenadas digitadas foram: (%.2f, %.2f)\n", x, y);
  getchar();

  

    return 0;
}
